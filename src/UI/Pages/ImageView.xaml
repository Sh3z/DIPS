<Window x:Class="DIPS.UI.Pages.ImageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DIPS.ViewModel.UserInterfaceVM;assembly=DIPS.ViewModel"
        xmlns:iv="clr-namespace:PannableContentControl;assembly=PannableContentControl"
        Title="ImageViewer">
    <Window.DataContext>
        <vm:ImageViewerViewModel/>
    </Window.DataContext>
    
    <Grid>
        <iv:PannableContentControl Background="White">
                <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
                <Image Source="{Binding SelectedImage, Mode=TwoWay}"/>
                </ScrollViewer>
        </iv:PannableContentControl>
    </Grid>
</Window>
